# Flask Core Settings
FLASK_APP=motopuppu:create_app() # アプリケーションファクトリを使用する場合
FLASK_ENV=development # development or production
SECRET_KEY= # ここに非常に長くランダムな秘密鍵を設定してください (例: openssl rand -hex 32 で生成)

# Database Settings
# 例 (SQLite): DATABASE_URI=sqlite:///../instance/database.db
# 例 (PostgreSQL): DATABASE_URI=postgresql://user:password@host:port/dbname
DATABASE_URI=sqlite:///../instance/database.db

# Misskey MiAuth Settings
MISSKEY_INSTANCE_URL=https://misskey.io # ログインに使用するMisskeyインスタンスのURL
# (MiAuthは基本的にこれだけでOK、クライアントID/Secretは不要)

# File Upload Settings (例)
# UPLOADED_PHOTOS_DEST=/path/to/upload/folder # アップロード先 (app.config['UPLOADED_PHOTOS_DEST']などで使用)
MAX_CONTENT_LENGTH=16 # MB単位: リクエスト全体の上限(ファイルサイズ制限と合わせて調整)

